{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Windows Build",
            "command": "${workspaceFolder}/build.bat", //workspace refers to root folder
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}